import type { type } from 'os'

export type BannerItem = {
  hrefUrl: string
  id: string
  imgUrl: string
  type: number
}
export type categoryItem = {
  id: 'string'
  name: 'string'
  icon: 'string'
}
export type PopularItem = {
  id: 'string'
  alt: 'string'
  pictures: ['string']
  target: 'string'
  title: 'string'
  type: 'string'
}
