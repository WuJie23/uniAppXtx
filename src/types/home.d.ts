import type { type } from 'os'

export type BannerItem = {
  hrefUrl: string
  id: string
  imgUrl: string
  type: number
}
export type categoryItem = {
  id: 'string'
  name: 'string'
  icon: 'string'
}
